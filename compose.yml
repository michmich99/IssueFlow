version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: issueflow-db
    environment:
      POSTGRES_DB: issueflow
      POSTGRES_USER: issueflow
      POSTGRES_PASSWORD: issueflow123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U issueflow"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
